function negatizer(numString){
  // var negNUm = numString * -1;
//  if(isNaN(negNUm)){
//   throw `input must be coercible to a number`
//  }
 return console.log(typeof numString);
}

negatizer(["cat"]); 